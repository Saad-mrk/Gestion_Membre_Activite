@model IEnumerable<Gestion_des_membres_et_activités_d_un_club.Models.Activite>

@{
    ViewData["Title"] = "Liste des activités";
}

<h2>Liste des activités</h2>

<p>
    <a asp-action="Create" class="btn btn-success">Créer une activité</a>
</p>

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Nom</th>
            <th>Description</th>
            <th>Date</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Nom</td>
                <td>@item.Description</td>
                <td>@item.DateActivite.ToString("dd/MM/yyyy")</td>
                <td>
                    <div class="d-flex gap-2">
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-success">Modifier</a>
                        <form asp-action="Delete" asp-route-id="@item.Id" method="post" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette activité ?');">
                            <button type="submit" class="btn btn-sm btn-outline-danger">Supprimer</button>
                        </form>
                    </div>
                </td>
            </tr>
        }
    </tbody>

</table>
                                                        