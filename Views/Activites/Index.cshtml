@model IEnumerable<Gestion_des_membres_et_activités_d_un_club.Models.Activite>

@{
    ViewData["Title"] = "Liste des activit�s";
}

<h2>Liste des activit�s</h2>

<p>
    <a asp-action="Create" class="btn btn-success">Cr�er une activit�</a>
</p>

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Nom</th>
            <th>Description</th>
            <th>Date</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Nom</td>
                <td>@item.Description</td>
                <td>@item.DateActivite.ToString("dd/MM/yyyy")</td>
                <td>
                    <div class="d-flex gap-2">
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-success">Modifier</a>
                        <form asp-action="Delete" asp-route-id="@item.Id" method="post" onsubmit="return confirm('�tes-vous s�r de vouloir supprimer cette activit� ?');">
                            <button type="submit" class="btn btn-sm btn-outline-danger">Supprimer</button>
                        </form>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
                                                        